@inherits YOYO.AspNetCore.ViewEngine.Razor.RazorViewTemplate<List<NET451OwinHost.Controlers.MyUser>>

@{
    Layout = "/Views/_Layout.cshtml";
}

<div>
    @foreach (var user in Model)
    {
        <p>@user.Name</p>
    }

</div>
